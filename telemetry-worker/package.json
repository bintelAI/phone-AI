{
  "name": "aries-telemetry-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply aries-telemetry --local",
    "db:remote": "wrangler d1 migrations apply aries-telemetry --remote"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.20.0"
  }
}
